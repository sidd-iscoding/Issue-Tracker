{
  "name": "pro-mern-stack",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "compile": "babel src --presets react --out-dir static",
    "compiles2015": "babel src --presets react,es2015 --out-dir static",
    "watch": "babel src --presets react --out-dir static --watch",
    "watches2015": "babel src --presets react,es2015 --out-dir static --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "^c"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^3.6.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1"
  },
  "description": ""
}
